'use client';
import Link from 'next/link';
import { UserAvatar } from '@/components/user-avatar';
interface ChatHeaderProps { name: string; avatarUrl: string | null; profileId: string; phone?: string | null; }
export function ChatHeader({ name, avatarUrl, profileId, phone }: ChatHeaderProps) { return <header className="shrink-0 border-b border-slate-200 bg-white px-4 py-3 text-slate-900 shadow-xs"><div className="mx-auto flex max-w-4xl items-center justify-between"><div className="flex min-w-0 items-center gap-3"><Link href="/messages" className="text-slate-600 hover:text-slate-900" aria-label="Back to messages">←</Link><Link href={`/sellers/${profileId}`} className="flex min-w-0 items-center gap-2.5"><UserAvatar id={profileId} name={name} avatarUrl={avatarUrl} className="h-9 w-9" /><span className="truncate text-base font-semibold">{name}</span></Link></div>{phone && <a href={`tel:${phone}`} aria-label="Call user" className="rounded-full p-2 text-slate-600 hover:bg-slate-100">☎</a>}</div></header>; }
